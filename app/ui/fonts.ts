import {Actor, Lusitana, Baloo_Da_2, Montserrat, Jost} from 'next/font/google'

export const actor = Actor({ subsets: ['latin'], weight: '400'})

export const lusitana = Lusitana({subsets: ['latin'], weight: ['400','700']})

export const baloo = Baloo_Da_2({subsets: ['latin'], weight: ['400','700']})

export const montserrat = Montserrat({subsets: ['latin'], weight: ['400',],})

export const jost = Jost({subsets: ['latin'], weight: ['400',],})

// export const acme = Acme({subsets: ['latin'], weight: ['400',],}) NO